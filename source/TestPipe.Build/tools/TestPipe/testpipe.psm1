#PROPERTIES

# INSTALL
function Install-TestPipe($package) {
  $id = $package.Id;
  $version = $package.Version;
}

# UNINSTALL
function Uninstall-TestPipe($package) {
  $id = $package.Id;
  $version = $package.Version;
}